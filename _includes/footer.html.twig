<footer class="site-footer border-t">
    <div class="container background-dark py-4 text-center">
        <div>
            <p class="m-0">
                <small>Website Copyright &copy; {{ 'now' | date('Y') }} {{ data.footer.copyright | escape }}</small>
            </p>
            <p class="m-0">
                <small>BZFlag Copyright &copy; 1993-{{ 'now' | date('Y') }} Tim Riker</small>
            </p>
        </div>

        <ul class="site-footer__links mb-0">
            {% for link in data.footer.links %}
                {%- set url = link.url[0] == '/' ? url(link.url) : link.url -%}
                <li>
                    <a href="{{ url }}">
                        {{- link.name -}}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
</footer>
