---
since: 2.4.2
category: Player Information
signatures:
    - - dataType: int
        name: playerId
        description: ~
        default: ~
returns:
    dataType: bool
    description: ~
---

Returns true if `playerID` is a human (and not something else, like a bot).
