---
section: documentation
sectiontitle: Developer Docs
permalink: /documentation/developer/bzfs_api/functions/
title: bzfs API Functions
navigation:
    prev: bzfs API
parent:
---

{% extends "_layouts/page-sidebar.html.twig" %}

{% set functions = collections.bzfs_api_functions | group('category') %}
{% set categories = functions | keys | sort %}

{% block introduction %}
    <h1 class="mt-0">BZFS API Functions</h1>
    <p>These API functions are allow plug-in developers to interact with the current game state and players.</p>
{% endblock %}

{% block aside %}
    <h2
        class="mt-0 c-accordion__header"
        data-role="accordion"
        data-target="#sidebar-toc"
        data-breakpoint="lg"
    >
        Categories
    </h2>

    <div id="sidebar-toc" class="c-accordion__body">
        <ul class="m-0">
            {% for category in categories %}
                <li><a href="#{{ category | slug }}">{{ category }}</a></li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}

{% block content %}
    {% for category in categories %}
        <h2 id="{{ category | slug }}">{{ category }}</h2>
        <ul>
            {% for function in functions[category] | order('filename') %}
                <li>
                    <a href="{{ url(function) }}"
                       class="text-decoration-none"
                    >
                        {{- function.basename -}}()
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endfor %}
{% endblock %}
